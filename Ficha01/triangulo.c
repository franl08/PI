#include <stdio.h>
#include "vertical.h"
#include "horizontal.h"
/* As alterações perante o enunciado são apenas para usar
 * a Makefile, dessa forma, para fazer o que pede no enunciado,
 * não seria necessário criar os .h, sendo que, seria apenas
 * copiar o que lá está feito.
 */
int main(){
	vertical(5);
	horizontal(5);
	return 0;
}
